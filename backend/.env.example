PORT=3001
MONGODB_URI=

# Stripe (optional — enables real test checkout in Chrome)
# Dashboard: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_...
# Optional: comma-separated extra origins (Expo tunnel, staging), e.g. https://abc.ngrok.io
# STRIPE_ALLOWED_ORIGINS=
